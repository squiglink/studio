services:
  studio:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 5173:5173
    command: pnpm vite
    volumes:
      - ./node_modules:/studio/node_modules
      - .:/studio
